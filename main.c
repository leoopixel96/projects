#include <stdio.h>

int main()
{
    /** ----------------------------------------------------
     * TipiSempliciIOEspressioni
     * ----------------------------------------------------- */

    /** ----------------------------------------------------
     * slide 19 
     * */
    // float num, abs;
    // int int_abs;
    // scanf_s("%f", &num);
    // abs = (num < 0) ? -num : num;
    // printf("abs = %f\n", abs);
    // int_abs = (int) abs; // !!!
    // printf("int_abs = %d\n", int_abs);
    // // corretto - funziona - in linea con sol - ok
    /**----------------------------------------------------- */

    /** ----------------------------------------------------
     * slide 21 
     * */
    // char c1, c2, c3, tmp;
    // scanf_s("%c%c%c", &c1, 1, &c2, 1, &c3, 1);
    // tmp = c1;
    // c1 < c3 ? 1 : (c1 = c3, c3 = tmp);
    // tmp = c1;
    // c1 < c2 ? 1 : (c1 = c2, c2 = tmp);
    // tmp = c2;
    // c2 < c3 ? 1 : (c2 = c3, c3 = tmp);
    // printf("c1 = %c c2 = %c c3 = %c", c1, c2, c3);
    // //funziona - in linea con sol - ok
    /**----------------------------------------------------- */

    /** ----------------------------------------------------
     * IFCicli
     * ----------------------------------------------------- */

    /** ----------------------------------------------------
     * slide 18
     * */
    // float t1, t2;
    // char op;
    // scanf_s("%d%c%d", &t1, &op, 1, &t2);
    // switch(op)
    // {
    //     case '+':
    //         printf("sum = %d\n", t1 + t2); break;
    //     case '-':
    //         printf("sub = %d\n", t1 - t2); break;
    //     case '/':
    //         printf("div = %f\n", t1 / t2); break;
    //     case '*':
    //         printf("sub = %d\n", t1 * t2); break; // !!! manca caso den = 0
    //     default:
    //         printf("error\n"); break;
    // }
    // // non funziona - non in linea con sol - nok
    /**----------------------------------------------------- */

    /** ----------------------------------------------------
     * slide 23
     * */
    // char c;

    // while(scanf_s("%c", &c,1) > 0 && c != '0')
    // {
    //     printf("c = %c\n", c);
    //     getchar();
    // }
    // // funziona - non in linea con sol - ok
    /**----------------------------------------------------- */

    /** ----------------------------------------------------
     * slide 32
     * */
    // int num, prod = 1, sum = 0;

    // while(scanf_s("%d", &num) > 0 && num != 0) 
    // {
    //     num < 0 ? (sum += num) : (prod *= num);
    //     printf("sum = %d prod = %d\n", sum, prod);
    // }
    // // funziona - non in linea con sol - ok
    /**----------------------------------------------------- */

    /** ----------------------------------------------------
     * slide 36
     * */
    int i, j, n, a, sum = 0, pot = 1;

    for(i = 0; i < n; i++)
    {
        for(j = 0; j < i; j++)
        {
            pot *= a;
        }
        sum += pot;
    }

    printf("due cicli: sum = %d\n", sum);

    for(i = 0; i < n; i++)
    {
        for(j = 0; j < i; j++)
        {
            pot *= a;
        }
        sum += pot;
    }




    /**----------------------------------------------------- */












    /** ----------------------------------------------------
     * 
     * ----------------------------------------------------- */

    /** ----------------------------------------------------
     * slide 
     * */

    /**----------------------------------------------------- */

    return 0;
}